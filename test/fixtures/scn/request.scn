const expect = require(`chai`).expect;

return () => request({
    method: `{{method}}` || `GET`,
    url: `{{host}}{{url}}`,
    followRedirect: false
  }).then(result => {
    expect(result.code).to.equals({{code}});
    return result.body;
  });
